# reservations

Reservation workflow — introduce a customer, hold a unit, escalate to sale.

Placeholder route: `/reservations`

Planned:

- Reservation lifecycle: PENDING -> APPROVED -> CANCELED -> CONVERTED.
- Agency-introduced-buyer protection window (configurable per project).
- Approval flow with permission checks (`reservation.approve`).
