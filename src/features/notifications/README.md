# notifications

Email and in-app notifications.

Not yet wired.

Planned:

- Notification preferences per user + per organization.
- Email templates in Serbian, single source of truth (see `server/auth/email.ts`).
- In-app inbox with mark-as-read and unread counts.
